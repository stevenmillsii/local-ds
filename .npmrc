# SETUP: Replace @YOUR_ORG with your GitHub org or username (e.g. @stevenmills).
# Set GITHUB_TOKEN in your environment or a .env file (never commit the token).
@YOUR_ORG:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
